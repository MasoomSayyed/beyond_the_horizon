using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundsRef : MonoBehaviour
{
    /* [SerializeField] private AudioClip audioSplashClip;
     [SerializeField] private AudioClip thunderCloudClip;


     private void Start()
     {
         WaterWave.Instance.OnWaterEntered += Instance_OnWaterEntered;
         ThunderClouds.Instance.OnCloudEntered += Instance_OnCloudEntered;
     }

     private void Instance_OnCloudEntered(object sender, System.EventArgs e)
     {
         AudioSource.PlayClipAtPoint(thunderCloudClip, Camera.main.transform.position, .02f);
     }

     private void Instance_OnWaterEntered(object sender, System.EventArgs e)
     {
         AudioSource.PlayClipAtPoint(audioSplashClip, Camera.main.transform.position, .02f);
     }
 */

}
